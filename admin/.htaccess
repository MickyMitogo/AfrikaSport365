# Allow all access (admin folder is protected by PHP login)
<FilesMatch "\.(php|json)$">
  Require all granted
</FilesMatch>

# Disable directory listing
Options -Indexes

# Allow all HTTP methods (GET, POST, PUT, DELETE)
<Limit GET POST PUT DELETE HEAD OPTIONS>
  Require all granted
</Limit>
